// chrome.storage.local.get("dataArray", function (result) {
//     cacheArray = result.dataArray;
//       if(cacheArray && Object.keys(cacheArray).length > 0){


//         chrome.storage.local.set({dataArray: allKeys}, function () {
    
//             chrome.storage.local.get('dataArray', function (result) {
//             });
//         });

//     }

// });